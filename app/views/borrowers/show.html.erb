<p id="notice"><%= notice %></p>

<div class="container">

  <div class="page-header">
    <h3><%= @borrower.name %></h3>
    <h4><%= @borrower.memo %><br>
      <div class="row">
        <div class="col-sm-8">
          <small><%= number_to_currency(@borrower.price) %></small>
        </div>
        <div class="col-sm-4">
          <%= link_to 'Edit', edit_borrower_path(@borrower), class: "btn btn-link" %> |
          <%= link_to 'Back', borrowers_path, class: "btn btn-link" %>
        </div>
      </div>
    </h4>
  </div>

</div>